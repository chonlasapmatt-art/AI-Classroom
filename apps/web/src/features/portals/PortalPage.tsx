import { useAuth } from '../../app/AuthContext';
const content = { assignments: ['งานและกิจกรรม', 'สร้าง เผยแพร่ ปิด และติดตามการส่งงานผ่าน trusted mutation boundary'], scores: ['คะแนนและเกรด', 'คะแนนที่เผยแพร่แล้วเท่านั้น พร้อมสูตรน้ำหนักและ conflict review'], reports: ['รายงาน', 'Student, Class, Attendance, Score, Grade, Missing Work และ At-Risk'], parents: ['ผู้ปกครอง', 'เชื่อมบัญชีด้วย one-time HMAC code + consent ตาม policy version'], settings: ['ตั้งค่า', 'นโยบายคะแนน ความเป็นส่วนตัว อุปกรณ์ และ environment'] } as const;
export function PortalPage({ kind }: { kind: keyof typeof content }) { const { active } = useAuth(); const [title, description] = content[kind]; return <><section className="page-heading"><div><span className="eyebrow">{active!.role.toUpperCase()} PORTAL</span><h1>{title}</h1><p>{description}</p></div></section><section className="panel"><div className="empty-state"><span>▦</span><h3>ยังไม่มีข้อมูลในขอบเขตนี้</h3><p>ระบบจะไม่สร้าง production demo data รายการจริงจะแสดงตาม RLS และสิทธิ์ของ {active!.displayName}</p></div></section></>; }
