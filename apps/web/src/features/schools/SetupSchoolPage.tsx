import { useState, type FormEvent } from 'react';
import { requireSupabase } from '../../services/supabase';
import { useAuth } from '../../app/AuthContext';

export function SetupSchoolPage() { const { signOut } = useAuth(); const [message, setMessage] = useState(''); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const values = Object.fromEntries(new FormData(event.currentTarget)); const { error } = await requireSupabase().functions.invoke('first-school-setup', { body: values }); setMessage(error ? error.message : 'สร้างโรงเรียนแล้ว กรุณาเข้าสู่ระบบใหม่'); } return <main className="setup-page"><form className="configuration-card" onSubmit={(event) => void submit(event)}><span className="eyebrow">FIRST SCHOOL SETUP</span><h1>ตั้งค่าโรงเรียนแรก</h1><p>บัญชีนี้ยังไม่มี membership ที่ใช้งาน กรุณาสร้างขอบเขตโรงเรียนจริง</p><div className="form-grid"><label>ชื่อโรงเรียน<input name="schoolName" required /></label><label>รหัสโรงเรียน<input name="schoolCode" pattern="[A-Za-z0-9-]{3,20}" required /></label><label>ปีการศึกษา<input name="academicYear" required /></label><label>ภาคเรียน<input name="term" required /></label></div><button className="primary-button">สร้างโรงเรียนและสิทธิ์ Admin</button>{message && <div className="alert">{message}</div>}<button type="button" className="text-button" onClick={() => void signOut()}>ออกจากระบบ</button></form></main>; }
