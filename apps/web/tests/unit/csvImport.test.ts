import {describe,expect,it} from 'vitest';import{previewStudentCsv}from '../../src/features/students/csvImport';
describe('student CSV preview',()=>{it('accepts BOM, UTF-8 Thai and quoted comma',()=>{const result=previewStudentCsv('\uFEFFstudent_code,display_name\n001,"เด็กหญิง ใจดี, ม.1"');expect(result.errors).toEqual([]);expect(result.rows[0]).toMatchObject({studentCode:'001',displayName:'เด็กหญิง ใจดี, ม.1'});});it('rejects invalid headers and duplicates without partial confirmation',()=>{expect(previewStudentCsv('code,name\n1,A').errors[0]?.rowNumber).toBe(1);const result=previewStudentCsv('student_code,display_name\n1,A\n1,B',new Set(['2']));expect(result.errors).toHaveLength(1);expect(result.rows).toHaveLength(1);});it('detects database duplicates',()=>{expect(previewStudentCsv('student_code,display_name\n2,A',new Set(['2'])).errors[0]?.message).toContain('มีในระบบแล้ว');});});
